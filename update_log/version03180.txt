已按你的口径改好，核心变化如下：

在 task_planner.py 中，direct_plan 现在改为：

先按 CLIP 概率排序
只要被“感知到”（默认 prob >= 0.5）就进计划
严重程度只用于排序，不再用于决定是否执行
若一个都没到 0.5，会兜底执行 top1（避免空计划）
Qwen2.5-VL-7B 兜底后，我加了保护：

兜底返回的计划如果漏掉了 CLIP 已感知任务，会自动补回去
确保“感知到了就要执行”的原则不被兜底模型破坏
你之前要求的“第 0 步必须执行首任务”改动仍然有效（在 restoration_agent.py）。

你现在这套行为可以总结为：

CLIP 感知到哪些退化 → 这些任务都要执行
概率大小只决定先后顺序
若 CLIP 不明确可用 Qwen2.5-VL-7B 重排，但不能删掉已感知任务
第一个任务不会在第 0 步被动态重规划取消